import 'package:flutter/material.dart';

class RoutActivity extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Rout Activity")),
      body: Center(child: Text("Rout Activity")),
    );
  }
}